import { FC } from "react";

interface RemoveBackgroundProps {
  selectImage: (image: string) => void;
}

const RemoveBackground: FC<RemoveBackgroundProps> = ({ selectImage }) => {
  return <></>;
};

export default RemoveBackground;
